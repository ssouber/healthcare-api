# Custom nginx server-block configuration (template/example)
# NOTE: All directives in this file are currently commented out and have no effect.
#       Uncomment and modify the examples below as needed for your environment.
# Files in this directory are included inside the nginx server {} block.
# Add your custom directives here.

# Example: increase upload size
# client_max_body_size 64M;

# Example: custom headers
# add_header X-Frame-Options "SAMEORIGIN" always;
# add_header X-Content-Type-Options "nosniff" always;
# add_header Content-Security-Policy "default-src 'self'; frame-ancestors 'self'; object-src 'none'; base-uri 'self';" always;
# add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;

# Example: gzip compression settings
# gzip on;
# gzip_vary on;
# gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

# Example: custom location block
# location /api/v2 {
#     try_files $uri $uri/ /index.php?$query_string;
# }

# Test endpoint to verify custom nginx config is loaded
# location /hello_world {
#     default_type text/plain;
#     return 200 "Hello World from custom nginx config!\n";
# }
